import { DygridDirective } from './dygrid.directive';

describe('DygridDirective', () => {
  it('should create an instance', () => {
    const directive = new DygridDirective();
    expect(directive).toBeTruthy();
  });
});
