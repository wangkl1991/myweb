import { HoverUnderlineDirective } from './hover-underline.directive';

describe('HoverUnderlineDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverUnderlineDirective();
    expect(directive).toBeTruthy();
  });
});
